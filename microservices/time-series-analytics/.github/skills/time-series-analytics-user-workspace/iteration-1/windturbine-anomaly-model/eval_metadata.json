{
  "eval_id": 3,
  "eval_name": "windturbine-anomaly-model",
  "prompt": "Deploy a pretrained anomaly-detection model for wind turbine sensor data through the Time Series Analytics microservice at microservices/time-series-analytics. A scikit-learn model (windturbine_anomaly_detector.pkl, an IsolationForest trained on a single numeric field) is provided as an input file -- wire it up for per-point inference rather than writing new detection logic. Name the UDF `windturbine_anomaly_detector`; the model file should be referenced in the deployment config, device set to CPU.\n\nValidate using the prebuilt intel/ia-time-series-analytics-microservice image via Docker Compose. Send a normal-range reading (e.g. field value 50) and an extreme outlier (e.g. field value 200) via POST /input and confirm only the outlier is flagged.",
  "assertions": [
    "A UDF file named windturbine_anomaly_detector.py exists that loads the provided .pkl model once in __init__ (or equivalent), not per-point",
    "The deployment package includes the PROVIDED windturbine_anomaly_detector.pkl file, not a newly trained model",
    "The config applied via POST /config references the model file under udfs.models and sets udfs.device to CPU",
    "Sending a normal-range reading (value near 50) does not get flagged",
    "Sending an outlier reading (value 200) gets flagged",
    "The UDF calls model.predict() and treats -1 as the anomaly convention consistent with IsolationForest"
  ],
  "files": ["windturbine_anomaly_detector.pkl"]
}
