{
  "eval_id": 2,
  "eval_name": "vibration-spike-mqtt-alert",
  "prompt": "Build a UDF that detects sudden vibration spikes on a motor and publishes an MQTT alert when one occurs, using the Time Series Analytics microservice at microservices/time-series-analytics. Field `vibration_mm_s` arrives via POST /input; flag a point if it jumps more than 5 mm/s from the previous reading. Wire the flagged output to an MQTT alert assuming the broker is named `motor_alerts_broker`. Name the UDF `vibration_spike_detector`.\n\nValidate using the prebuilt intel/ia-time-series-analytics-microservice image via Docker Compose, with MQTT alerting configured. Send this sequence to POST /input: vibration_mm_s = 2.0, then 2.1, then 9.5 (the jump from 2.1 to 9.5 should flag), then 9.6 (should not flag -- small delta from the now-elevated baseline).",
  "assertions": [
    "A UDF file named vibration_spike_detector.py exists that tracks a previous value across point() calls as instance state",
    "A tick script named vibration_spike_detector.tick exists that references the vibration_spike_detector UDF node",
    "The tick script includes an alert node configured for MQTT broker motor_alerts_broker (not just a bare UDF node)",
    "The deployment package was successfully uploaded and config applied via the REST API",
    "Sending the sequence 2.0, 2.1, 9.5 results in the 9.5 point being flagged (delta from 2.1 exceeds 5 mm/s)",
    "Sending 9.6 immediately after 9.5 does NOT get flagged (small delta from the now-elevated baseline)",
    "The rate-of-change threshold used is 5 mm/s as specified, not an arbitrary different value"
  ]
}
