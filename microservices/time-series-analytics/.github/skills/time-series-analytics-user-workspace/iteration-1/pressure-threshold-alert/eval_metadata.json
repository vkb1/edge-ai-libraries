{
  "eval_id": 1,
  "eval_name": "pressure-threshold-alert",
  "prompt": "Build a UDF that flags hydraulic pressure readings outside a safe operating band, using the Time Series Analytics microservice at microservices/time-series-analytics. Field `pressure_bar` arrives via POST /input; flag any reading below 80 or above 150. Name the UDF `pressure_guard`. Deploy it against a running instance of the service and confirm flagged points show up in the container logs.\n\nValidate using the prebuilt intel/ia-time-series-analytics-microservice image via Docker Compose, and these sample input points: {\"topic\": \"point_data\", \"fields\": {\"pressure_bar\": 65}} (should flag) and {\"topic\": \"point_data\", \"fields\": {\"pressure_bar\": 110}} (should not flag).",
  "assertions": [
    "A UDF file named pressure_guard.py exists implementing threshold detection logic",
    "A tick script named pressure_guard.tick exists and its UDF node invokes @pressure_guard()",
    "The deployment package was successfully uploaded via POST /udfs/package (not just written locally)",
    "Config was successfully applied naming udfs.name as pressure_guard via POST /config",
    "Sending pressure_bar=65 produces a flagged/logged anomaly in the container logs",
    "Sending pressure_bar=110 does NOT produce a flagged anomaly in the container logs",
    "The UDF reads the pressure_bar field specifically, not a different or generic field name"
  ]
}
