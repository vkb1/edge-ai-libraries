# Copyright (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

FROM node:22.14.0 AS vue-app

RUN apt-get update -y && apt-get install -y --no-install-recommends git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /home/user/ui

ENV TMPFILE_PATH=/home/user/ui
ENV NODE_OPTIONS=--max-old-space-size=4096
ENV NPM_CONFIG_AUDIT=false
ENV NPM_CONFIG_FUND=false

COPY ./ui/package*.json ./
RUN npm install

COPY ./ui ./
RUN npm run build

RUN useradd -m -u 1001 user && chown -R user:user /home/user/ui

FROM nginx:stable-alpine

RUN adduser -D -u 1001 user

RUN mkdir -p /var/cache/nginx /var/log/nginx /run/nginx && \
    chown -R user:user /var/cache/nginx /var/log/nginx /run/nginx && \
    sed -i \
        -e '/^user[[:space:]].*;/d' \
        -e 's!^pid[[:space:]].*!pid /run/nginx/nginx.pid;!' \
        /etc/nginx/nginx.conf

COPY --from=vue-app --chown=user:user /home/user/ui/dist /usr/share/nginx/html

RUN rm -f /etc/nginx/conf.d/default.conf && \
    chown user:user /etc/nginx/conf.d

COPY ./ui/docker/nginx.conf /etc/nginx/templates/default.conf.template

USER user

EXPOSE 7010
CMD ["nginx", "-g", "daemon off;"]