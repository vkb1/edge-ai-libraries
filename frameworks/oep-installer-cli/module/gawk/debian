
debian_05_install_gawk () {
  if dpkg-query -L gawk > /dev/null 2>&1; then
    echo "Gawk is already installed. Skipping..."
  else
    ensure_apt update
    ensure_apt install --no-install-recommends -y gawk
  fi
}

